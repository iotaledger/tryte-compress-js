/// <reference types="node" />
/**
 * Decompress the buffer back to a tryte string.
 * @param buffer The buffer to decompress.
 * @returns The tryte string.
 */
export declare function decompress(buffer: Buffer): string;
